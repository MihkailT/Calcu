#include<iostream>
#include<cmath>
#include<stdexcept>

// Р¤СѓРЅРєС†РёРё РґР»СЏ Р°СЂРёС„РјРµС‚РёС‡РµСЃРєРёС… РѕРїРµСЂР°С†РёР№
double СЃР»РѕР¶РµРЅРёРµ(double a, double b) {
    return a + b; // РЎРєР»Р°РґС‹РІР°РµРј РґРІР° С‡РёСЃР»Р°

double РІС‹С‡РёС‚Р°РЅРёРµ(double a, double b) {
    return a - b; // Р’С‹С‡РёС‚Р°РµРј РІС‚РѕСЂРѕРµ С‡РёСЃР»Рѕ РёР· РїРµСЂРІРѕРіРѕ
}

double СѓРјРЅРѕР¶РµРЅРёРµ(double a, double b) {
    return a * b; // РЈРјРЅРѕР¶Р°РµРј РґРІР° С‡РёСЃР»Р°
}

double РґРµР»РµРЅРёРµ(double a, double b) {
    if (b == 0) {
        throw std::invalid_argument("РћС€РёР±РєР°: Р”РµР»РµРЅРёРµ РЅР° РЅРѕР»СЊ РЅРµ РґРѕРїСѓСЃРєР°РµС‚СЃСЏ."); // РџСЂРѕРІРµСЂСЏРµРј РґРµР»РµРЅРёРµ РЅР° РЅРѕР»СЊ
    }
    return a / b; // Р”РµР»РёРј РїРµСЂРІРѕРµ С‡РёСЃР»Рѕ РЅР° РІС‚РѕСЂРѕРµ
}
=======
std::pair<double, double> РїРѕР»СѓС‡РёС‚СЊР§РёСЃР»Р°() {
    double С‡РёСЃР»Рѕ1, С‡РёСЃР»Рѕ2;
    std::cout << "Р’РІРµРґРёС‚Рµ РґРІР° С‡РёСЃР»Р°: "; // РџСЂРѕСЃРёРј РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ РІРІРµСЃС‚Рё РґРІР° С‡РёСЃР»Р°
    std::cin >> С‡РёСЃР»Рѕ1 >> С‡РёСЃР»Рѕ2;
    return { С‡РёСЃР»Рѕ1, С‡РёСЃР»Рѕ2 };
}

char РїРѕР»СѓС‡РёС‚СЊРћРїРµСЂР°С†РёСЋ() {
    char РѕРїРµСЂР°С†РёСЏ;
    std::cout << "Р’РІРµРґРёС‚Рµ РѕРїРµСЂР°С†РёСЋ (+, -, *, /): "; // РџСЂРѕСЃРёРј РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ РІС‹Р±СЂР°С‚СЊ РѕРїРµСЂР°С†РёСЋ
    std::cin >> РѕРїРµСЂР°С†РёСЏ;
    return РѕРїРµСЂР°С†РёСЏ;

    void РІС‹РІРµСЃС‚РёР РµР·СѓР»СЊС‚Р°С‚(double СЂРµР·СѓР»СЊС‚Р°С‚) {
        std::cout << "Р РµР·СѓР»СЊС‚Р°С‚: " << СЂРµР·СѓР»СЊС‚Р°С‚ << std::endl; // Р’С‹РІРѕРґРёРј СЂРµР·СѓР»СЊС‚Р°С‚
    }
int main() {
    // Калькулятор
    std::pair<double, double> числа = получитьЧисла();
    char операция = получитьОперацию();

    try {
        switch (операция) {
        case '+':
            вывестиРезультат(сложение(числа.first, числа.second)); // Выполняем сложение
            break;
        case '-':
            вывестиРезультат(вычитание(числа.first, числа.second)); // Выполняем вычитание
            break;
        case '*':
            вывестиРезультат(умножение(числа.first, числа.second)); // Выполняем умножение
            break;
        case '/':
            вывестиРезультат(деление(числа.first, числа.second)); // Выполняем деление
            break;
        default:
            std::cout << "Неверная операция.\n"; // Обрабатываем неверную операцию
        }
    }
    catch (const std::exception& e) {
        std::cerr << e.what() << std::endl;
    }

};