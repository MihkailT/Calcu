#include<iostream>
#include<cmath>
#include<stdexcept>

// –§—É–Ω–∫—Ü–∏–∏ –¥–ª—è –≤–≤–æ–¥–∞ –∏ –≤—ã–≤–æ–¥–∞
std::pair<double, double> –ø–æ–ª—É—á–∏—Ç—å–ß–∏—Å–ª–∞() {
    double —á–∏—Å–ª–æ1, —á–∏—Å–ª–æ2;
    std::cout << "–í–≤–µ–¥–∏—Ç–µ –¥–≤–∞ —á–∏—Å–ª–∞: "; // –ü—Ä–æ—Å–∏–º –ø–æ–ª—å–∑–æ–≤–∞—Ç–µ–ª—è –≤–≤–µ—Å—Ç–∏ –¥–≤–∞ —á–∏—Å–ª–∞
    std::cin >> —á–∏—Å–ª–æ1 >> —á–∏—Å–ª–æ2;
    return { —á–∏—Å–ª–æ1, —á–∏—Å–ª–æ2 };
}

char –ø–æ–ª—É—á–∏—Ç—å–û–ø–µ—Ä–∞—Ü–∏—é() {
    char –æ–ø–µ—Ä–∞—Ü–∏—è;
    std::cout << "–í–≤–µ–¥–∏—Ç–µ –æ–ø–µ—Ä–∞—Ü–∏—é (+, -, *, /): "; // –ü—Ä–æ—Å–∏–º –ø–æ–ª—å–∑–æ–≤–∞—Ç–µ–ª—è –≤—ã–±—Ä–∞—Ç—å –æ–ø–µ—Ä–∞—Ü–∏—é
    std::cin >> –æ–ø–µ—Ä–∞—Ü–∏—è;
    return –æ–ø–µ—Ä–∞—Ü–∏—è;
}

void –≤—ã–≤–µ—Å—Ç–∏–†–µ–∑—É–ª—å—Ç–∞—Ç(double —Ä–µ–∑—É–ª—å—Ç–∞—Ç) {
    std::cout << "–†–µ–∑—É–ª—å—Ç–∞—Ç: " << —Ä–µ–∑—É–ª—å—Ç–∞—Ç << std::endl; // –í—ã–≤–æ–¥–∏–º —Ä–µ–∑—É–ª—å—Ç–∞—Ç
}

// –§—É–Ω–∫—Ü–∏–∏ –¥–ª—è –∞—Ä–∏—Ñ–º–µ—Ç–∏—á–µ—Å–∫–∏—Ö –æ–ø–µ—Ä–∞—Ü–∏–π
double —Å–ª–æ–∂–µ–Ω–∏–µ(double a, double b) {
    return a + b; // –°–∫–ª–∞–¥—ã–≤–∞–µ–º –¥–≤–∞ —á–∏—Å–ª–∞
}

double –≤—ã—á–∏—Ç–∞–Ω–∏–µ(double a, double b) {
    return a - b; // –í—ã—á–∏—Ç–∞–µ–º –≤—Ç–æ—Ä–æ–µ —á–∏—Å–ª–æ –∏–∑ –ø–µ—Ä–≤–æ–≥–æ
}

double —É–º–Ω–æ–∂–µ–Ω–∏–µ(double a, double b) {
    return a * b; // –£–º–Ω–æ–∂–∞–µ–º –¥–≤–∞ —á–∏—Å–ª–∞
}

double –¥–µ–ª–µ–Ω–∏–µ(double a, double b) {
    if (b == 0) {
        throw std::invalid_argument("–û—à–∏–±–∫–∞: –î–µ–ª–µ–Ω–∏–µ –Ω–∞ –Ω–æ–ª—å –Ω–µ –¥–æ–ø—É—Å–∫–∞–µ—Ç—Å—è."); // –ü—Ä–æ–≤–µ—Ä—è–µ–º –¥–µ–ª–µ–Ω–∏–µ –Ω–∞ –Ω–æ–ª—å
    }
    return a / b; // –î–µ–ª–∏–º –ø–µ—Ä–≤–æ–µ —á–∏—Å–ª–æ –Ω–∞ –≤—Ç–æ—Ä–æ–µ
}

// ‘ÛÌÍˆËË ‰Îˇ ˛ÌËÚ-ÚÂÒÚÓ‚
void ÚÂÒÚ_ÒÎÓÊÂÌËÂ() {
    double ÂÁÛÎ¸Ú‡Ú = ÒÎÓÊÂÌËÂ(2, 3);
    if (fabs(ÂÁÛÎ¸Ú‡Ú - 5) < 1e-9) {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ÒÎÓÊÂÌËˇ ÔÓÈ‰ÂÌ.\n"; // œÓ‚ÂˇÂÏ ÍÓÂÍÚÌÓÒÚ¸ ÒÎÓÊÂÌËˇ
    }
    else {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ÒÎÓÊÂÌËˇ ÌÂ ÔÓÈ‰ÂÌ. ŒÊË‰‡ÂÏ˚È ÂÁÛÎ¸Ú‡Ú: 5, ‘‡ÍÚË˜ÂÒÍËÈ ÂÁÛÎ¸Ú‡Ú: " << ÂÁÛÎ¸Ú‡Ú << "\n";
    }
}

void ÚÂÒÚ_‚˚˜ËÚ‡ÌËÂ() {
    double ÂÁÛÎ¸Ú‡Ú = ‚˚˜ËÚ‡ÌËÂ(5, 3);
    if (fabs(ÂÁÛÎ¸Ú‡Ú - 2) < 1e-9) {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ‚˚˜ËÚ‡ÌËˇ ÔÓÈ‰ÂÌ.\n"; // œÓ‚ÂˇÂÏ ÍÓÂÍÚÌÓÒÚ¸ ‚˚˜ËÚ‡ÌËˇ
    }
    else {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ‚˚˜ËÚ‡ÌËˇ ÌÂ ÔÓÈ‰ÂÌ. ŒÊË‰‡ÂÏ˚È ÂÁÛÎ¸Ú‡Ú: 2, ‘‡ÍÚË˜ÂÒÍËÈ ÂÁÛÎ¸Ú‡Ú: " << ÂÁÛÎ¸Ú‡Ú << "\n";
    }
}

void ÚÂÒÚ_ÛÏÌÓÊÂÌËÂ() {
    double ÂÁÛÎ¸Ú‡Ú = ÛÏÌÓÊÂÌËÂ(2, 3);
    if (fabs(ÂÁÛÎ¸Ú‡Ú - 6) < 1e-9) {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ÛÏÌÓÊÂÌËˇ ÔÓÈ‰ÂÌ.\n"; // œÓ‚ÂˇÂÏ ÍÓÂÍÚÌÓÒÚ¸ ÛÏÌÓÊÂÌËˇ
    }
    else {
        std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ÛÏÌÓÊÂÌËˇ ÌÂ ÔÓÈ‰ÂÌ. ŒÊË‰‡ÂÏ˚È ÂÁÛÎ¸Ú‡Ú: 6, ‘‡ÍÚË˜ÂÒÍËÈ ÂÁÛÎ¸Ú‡Ú: " << ÂÁÛÎ¸Ú‡Ú << "\n";
    }
}

void ÚÂÒÚ_‰ÂÎÂÌËÂ() {
    try {
        double ÂÁÛÎ¸Ú‡Ú = ‰ÂÎÂÌËÂ(6, 3);
        if (fabs(ÂÁÛÎ¸Ú‡Ú - 2) < 1e-9) {
            std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ‰ÂÎÂÌËˇ ÔÓÈ‰ÂÌ.\n"; // œÓ‚ÂˇÂÏ ÍÓÂÍÚÌÓÒÚ¸ ‰ÂÎÂÌËˇ
        }
        else {
            std::cout << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ‰ÂÎÂÌËˇ ÌÂ ÔÓÈ‰ÂÌ. ŒÊË‰‡ÂÏ˚È ÂÁÛÎ¸Ú‡Ú: 2, ‘‡ÍÚË˜ÂÒÍËÈ ÂÁÛÎ¸Ú‡Ú: " << ÂÁÛÎ¸Ú‡Ú << "\n";
        }
    }
    catch (const std::exception& e) {
        std::cerr << "“ÂÒÚ ‰Îˇ ÙÛÌÍˆËË ‰ÂÎÂÌËˇ ÌÂ ÔÓÈ‰ÂÌ. »ÒÍÎ˛˜ÂÌËÂ: " << e.what() << "\n";
    }
}

int main() {
    // –ö–∞–ª—å–∫—É–ª—è—Ç–æ—Ä
    std::pair<double, double> —á–∏—Å–ª–∞ = –ø–æ–ª—É—á–∏—Ç—å–ß–∏—Å–ª–∞();
    char –æ–ø–µ—Ä–∞—Ü–∏—è = –ø–æ–ª—É—á–∏—Ç—å–û–ø–µ—Ä–∞—Ü–∏—é();

    try {
        switch (–æ–ø–µ—Ä–∞—Ü–∏—è) {
        case '+':
            –≤—ã–≤–µ—Å—Ç–∏–†–µ–∑—É–ª—å—Ç–∞—Ç(—Å–ª–æ–∂–µ–Ω–∏–µ(—á–∏—Å–ª–∞.first, —á–∏—Å–ª–∞.second)); // –í—ã–ø–æ–ª–Ω—è–µ–º —Å–ª–æ–∂–µ–Ω–∏–µ
            break;
        case '-':
            –≤—ã–≤–µ—Å—Ç–∏–†–µ–∑—É–ª—å—Ç–∞—Ç(–≤—ã—á–∏—Ç–∞–Ω–∏–µ(—á–∏—Å–ª–∞.first, —á–∏—Å–ª–∞.second)); // –í—ã–ø–æ–ª–Ω—è–µ–º –≤—ã—á–∏—Ç–∞–Ω–∏–µ
            break;
        case '*':
            –≤—ã–≤–µ—Å—Ç–∏–†–µ–∑—É–ª—å—Ç–∞—Ç(—É–º–Ω–æ–∂–µ–Ω–∏–µ(—á–∏—Å–ª–∞.first, —á–∏—Å–ª–∞.second)); // –í—ã–ø–æ–ª–Ω—è–µ–º —É–º–Ω–æ–∂–µ–Ω–∏–µ
            break;
        case '/':
            –≤—ã–≤–µ—Å—Ç–∏–†–µ–∑—É–ª—å—Ç–∞—Ç(–¥–µ–ª–µ–Ω–∏–µ(—á–∏—Å–ª–∞.first, —á–∏—Å–ª–∞.second)); // –í—ã–ø–æ–ª–Ω—è–µ–º –¥–µ–ª–µ–Ω–∏–µ
            break;
        default:
            std::cout << "–ù–µ–≤–µ—Ä–Ω–∞—è –æ–ø–µ—Ä–∞—Ü–∏—è.\n"; // –û–±—Ä–∞–±–∞—Ç—ã–≤–∞–µ–º –Ω–µ–≤–µ—Ä–Ω—É—é –æ–ø–µ—Ä–∞—Ü–∏—é.
        }
    }
    catch (const std::exception& e) {
        std::cerr << e.what() << std::endl;
    }

    // «‡ÔÛÒÍ ˛ÌËÚ-ÚÂÒÚÓ‚
    ÚÂÒÚ_ÒÎÓÊÂÌËÂ();
    ÚÂÒÚ_‚˚˜ËÚ‡ÌËÂ();
    ÚÂÒÚ_ÛÏÌÓÊÂÌËÂ();
    ÚÂÒÚ_‰ÂÎÂÌËÂ();

    return 0;
}

